---
description: Build the nat-os image.
agent: os-dev
---

Build the nat-os kernel image:

$ARGUMENTS

## Instructions

1. Run `.\build.ps1` with any arguments provided.
2. If the build fails, analyze the compiler/linker error and fix it.
3. Report the image size and any warnings on success.
